学习目标
掌握函数返回值的用法

课程内容
a.返回值的定义
b.return语句

a.返回值定义
函数有些时候是直接输出，有些时候需要处理一些参数，根据参数进行一些操作，需要返回数据给调用者，称为返回值，这样必须使用return语句

b.return语句
用于退出函数，选择性向调用者返回一个表达式，直接return 返回None
一个返回值
  def sum(a,b):
    suan=a+b
    return suan
  s=sum(20,30)
  print(s)
  
多个返回值
  def re(a,b):
    a*=10
    b*=10
    return a,b
  num=re(3,5)  #如果返回多个值，并且存在一个变量中，以元组形式保存
  print(type(num))
  r1,r2=re(3,4)
  print(r1,r2)
  
  
